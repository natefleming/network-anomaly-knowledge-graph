command:
  - "bash"
  - "-lc"
  - "uvicorn server.app:app --host 0.0.0.0 --port ${DATABRICKS_APP_PORT:-8000}"

env:
  - name: DATABRICKS_WAREHOUSE_ID
    valueFrom: warehouse
  - name: DATABRICKS_CATALOG
    value: retail_consumer_goods
  - name: DATABRICKS_SCHEMA
    value: network_anomaly_graph
